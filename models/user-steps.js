class UserStep {
  constructor(id, date, steps, calories) {
    this.id = id
    this.name = name
    this.date = date
    this.steps = steps
    this.calories = calories
  }
}

export default UserStep
